# 给 OpenCode / DeepSeek 的任务

请读取本目录 README.md、PRD.md、DESIGN.md、PLAN.md，并实际打开 SELECTED_DESIGN.png。

目标项目：C:\Users\18041\Desktop\della-pages\Ceiling Cassette vs Wall Mount Mini Split

用户选中了这张长图。请在当前 HTML 上完整落实已限定的 Hero、选型器、比较、FAQ 内容和页尾改动；你负责编码和测试，Codex 负责视觉审核。不要改用此前的 v2–v6 或其他 FINAL 任务，也不要重新设计页面。

先备份并记录现有 #products/#services 基线。所选图保留左场景右问题；不要换成白底设备图。图片里的 yes/yes/look → wall mount 是错误示例，要按 PRD 矩阵给 cassette 推荐。用户选定的是视觉布局，不是错误结果。

现有四-tab商品模块和 Services 原样保留，不照生图重做。FAQ 沿用原样式改为 PRD 五条问答。Hero 不加 bar，不拼图，只一个按钮。页尾只短标题和两按钮，不做产品入口卡。比较区使用 PRD 六行，无假下拉、隐藏行和大面积深蓝背景。

本包有独立 Hero 和选型示意素材。使用项目本地字体和真实产品图。图片是参考，不要把整张设计稿贴进网页。选型示意必须带 Alternative installation locations, illustrated together. Not an installation drawing. 图注。

按 PLAN 完成实现与真实浏览器验证，输出截图文件的绝对路径、27组合测试报告、FAQ/JSON-LD一致性、各视口检查、商品/Services保留证据、修改文件清单和未完成项。

完成后停在本地，交回 Codex 做视觉审核。不要提交、发布或把未完成测试写成已通过。
